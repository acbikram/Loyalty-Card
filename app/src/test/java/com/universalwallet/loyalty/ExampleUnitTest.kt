package com.universalwallet.loyalty

import com.google.common.truth.Truth.assertThat
import org.junit.Test

/**
 * Sanity unit test confirming the JUnit + Truth toolchain is wired up. Feature
 * test suites are added alongside their features in later phases.
 */
class ExampleUnitTest {

    @Test
    fun arithmetic_isCorrect() {
        assertThat(2 + 2).isEqualTo(4)
    }
}
